{
  "name": "cisco-navigator-panel",
  "version": "0.1.0",
  "private": true,
  "description": "Smart home, media and photo panel for the Cisco Room Navigator (RoomOS)",
  "license": "MIT",
  "workspaces": [
    "panel",
    "server"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:panel": "npm run dev --workspace panel",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace panel && npm run build --workspace server",
    "start": "npm run start --workspace server",
    "typecheck": "npm run typecheck --workspace panel && npm run typecheck --workspace server",
    "clean": "rm -rf panel/dist server/dist node_modules/.vite",
    "test": "npm run build --workspace server && node --test 'server/test/*.test.mjs'"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
